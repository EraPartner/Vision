{
  "skills": ["release"],
  "query": "I changed the packaged native attachment directory. What else do I need to verify before this ships?",
  "files": [],
  "expected_behavior": [
    "Identifies backup, restore, and upgrade compatibility for the existing attachment directory",
    "Explicitly references the v1.0.2 data-loss precedent as the reason this sync matters, not just as a generic best practice",
    "Flags persistence compatibility as a release gate, not a one-time fix",
    "Does not treat the path edit as sufficient without persistence verification"
  ]
}
