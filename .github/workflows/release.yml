name: Build images & Mac app

permissions:
  contents: read
  packages: write

on:
  push:
    tags:
      - 'v*'
    branches:
      - main

env:
  IMAGE_REPO: ghcr.io/${{ toLower(github.repository_owner) }}/vision
  IMAGE_TAG: ${{ github.sha }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build_and_push_images:
    name: Build and push multi-arch images
    runs-on: ubuntu-latest
    outputs:
      image_full: ${{ steps.set_tag.outputs.image_full }}
    steps:
      - uses: actions/checkout@v4

      - name: Set image tag
        id: set_tag
        run: |
          # Compose the image tag and expose it as a step output via GITHUB_OUTPUT
          echo "image_full=${{ env.IMAGE_REPO }}:${{ env.IMAGE_TAG }}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT || github.token }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_REPO }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_REPO }}:latest

  build_mac_app:
    name: Build macOS DMG
    needs: build_and_push_images
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          export BUN_INSTALL="$HOME/.bun"
          export PATH="$BUN_INSTALL/bin:$PATH"
        shell: bash

      - name: Prepare embedded docker-compose with concrete image
        run: |
          cd packaging/electron/resources
          # Read image_full from the build job output and extract its tag
          IMAGE_FULL="${{ needs.build_and_push_images.outputs.image_full }}"
          if [ -z "$IMAGE_FULL" ]; then
            echo "Failed to read image_full from previous job" >&2
            exit 1
          fi
          IMAGE_TAG="${IMAGE_FULL##*:}"
          echo "APP_IMAGE_TAG=${IMAGE_TAG}" > .compose.env
          cd -
        shell: bash

      - name: Install dependencies & build
        run: |
          cd packaging/electron
          bun install
          bun run dist
        shell: bash

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: vision-macos-dmg
          path: packaging/electron/dist/*.dmg

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload DMG to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: packaging/electron/dist/*.dmg
          asset_name: vision-${{ github.ref_name }}.dmg
          asset_content_type: application/x-apple-diskimage
