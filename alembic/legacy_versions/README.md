This folder contains backed-up legacy Alembic revision scripts that were removed from the active `alembic/versions/` directory.

We moved these files because the repository owner wanted to reset the Alembic history and create a clean new baseline.

If you need to restore any legacy revision, copy the corresponding file back into `alembic/versions/` and run Alembic commands as needed.
