{
  "name": "vault-voyager",
  "private": true,
  "version": "1.0.0",
  "description": "Financial transaction management application",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "apps/frontend",
    "apps/node-backend"
  ],
  "scripts": {
    "install:all": "bun install",
    "dev": "concurrently --names \"backend,frontend\" -c \"blue,green\" \"bun run --filter 'financial-transaction-manager-node' dev\" \"bun run --filter 'vault-voyager-frontend' dev\"",
    "build": "bun run --filter 'vault-voyager-frontend' build",
    "build:dev": "bun run --filter 'vault-voyager-frontend' build:dev",
    "preview": "bun run --filter 'vault-voyager-frontend' preview",
    "lint": "bun run --filter 'vault-voyager-frontend' lint",
    "backend": "bun run --filter 'financial-transaction-manager-node' start",
    "test": "bun run --filter 'financial-transaction-manager-node' test",
    "test:watch": "bun run --filter 'financial-transaction-manager-node' test:watch",
    "db:setup": "bash scripts/db/setup_postgres.sh",
    "db:upgrade": "alembic -c config/alembic.ini upgrade head",
    "db:downgrade": "alembic -c config/alembic.ini downgrade -1",
    "db:current": "alembic -c config/alembic.ini current",
    "db:history": "alembic -c config/alembic.ini history",
    "db:stamp": "alembic -c config/alembic.ini stamp head",
    "db:revision": "alembic -c config/alembic.ini revision --autogenerate -m",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f app",
    "docker:rebuild": "docker compose down && docker compose build --no-cache && docker compose up -d",
    "update": "bun update"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "rollup": ">=4.59.0"
  },
  "resolutions": {
    "rollup": "4.59.0"
  }
}