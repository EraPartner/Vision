/**
 * Saved-charts service — the route-facing seam over savedChartsRepository
 * (eslint vision-local/no-repo-direct-from-route).
 */
export { default } from '../repositories/savedChartsRepository.js';
