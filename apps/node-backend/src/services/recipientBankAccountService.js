/**
 * Recipient bank-account service — the route-facing seam over
 * recipientBankAccountRepository (eslint vision-local/no-repo-direct-from-route).
 */
export { default } from '../repositories/recipientBankAccountRepository.js';
